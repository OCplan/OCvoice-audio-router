name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

env:
  # The engine tag and the three digests below are ONE unit: bumping the engine
  # means changing the tag and all three ENGINE_SHA256_* values in the same commit.
  #
  # Pinned, not floating, for two reasons. (1) These assets live in OCplan/OCvoice,
  # the main product repo — "latest" there follows whatever release was cut most
  # recently, so the first OCvoice APP release moves it to a release carrying no
  # engine assets at all and every build here breaks on an empty --pattern match.
  # (2) The engine is nested-signed into the shipped .app with our Developer ID:
  # whatever bytes land here go out as ours, so the build must state up front
  # exactly which bytes it is willing to sign.
  #
  # There is deliberately no repo-var or dispatch-input override: an override
  # would change the signed payload without changing this file, and would
  # invalidate the digests below with no diff to review.
  ENGINE_VERSION: restream-engine-v0.1.0
  ENGINE_SHA256_DARWIN_ARM64: 8dbbaec4f6aef3943b5d25591b411337062863c65f5653938036125b412a965f
  ENGINE_SHA256_DARWIN_X64: 02fdfa282c14eb4f8c50badc18c51fcea86103fe94ef4ae1e2e50f3c293529d4
  ENGINE_SHA256_WINDOWS_X64: a4bfa3c02f103d5cfbbd96821fb76dd167d3ce50620549af520cb377e702a381

jobs:
  # Refuse to build anything whose version numbers disagree, before the matrix
  # spends a signed, notarized build on it. `release` depends on this job too,
  # so dropping it from `build` alone cannot re-open the hole. The check lives
  # in a script rather than inline here so it can be run — and re-verified after
  # it is edited — without pushing a tag to a public repo.
  version-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify tag, Cargo.toml and Info.plist agree
        run: scripts/check-version-tag.sh

  build:
    needs: version-gate
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: OCvoice-Audio-Router-macOS-arm64
            binary: ocvoice-audio-router
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: OCvoice-Audio-Router-macOS-x64
            binary: ocvoice-audio-router
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: OCvoice-Audio-Router-Windows
            binary: ocvoice-audio-router.exe

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
          cache-targets: ${{ runner.os != 'macOS' }}

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }}

      # The repo's tests must actually run: a test CI never executes is
      # documentation, not a gate. --release reuses what the step above just
      # built, so this costs the test-harness compile and nothing more, and it
      # runs before the sidecars, the signature and the notarization.
      #
      # x86_64-apple-darwin is excluded because that slice is cross-compiled on
      # an arm64 runner and cannot execute its own test binaries; the same tests
      # run natively in the arm64 slice. The supervisor test is
      # #[cfg(all(test, unix))], so it compiles out on Windows rather than
      # failing it.
      - name: Run tests
        if: matrix.target != 'x86_64-apple-darwin'
        run: cargo test --release --target ${{ matrix.target }}

      # -D warnings, because a clippy that only warns gates nothing. Confined to
      # the arm64 macOS slice: that is the configuration it has been shown clean
      # on. The Windows-only cfg paths have never been linted, so widening this
      # belongs in a change that first shows it green there.
      - name: Clippy
        if: matrix.target == 'aarch64-apple-darwin'
        run: cargo clippy --all-targets --target ${{ matrix.target }} -- -D warnings

      # DMG creation duplicates the signed bundle and needs space beyond the
      # final image. Keep the executable, then discard compiler intermediates.
      - name: Stage macOS executable and free build space
        if: runner.os == 'macOS'
        shell: bash
        env:
          BUILD_TARGET: ${{ matrix.target }}
          BUILD_BINARY: ${{ matrix.binary }}
        run: |
          set -euo pipefail
          mkdir -p .release-router
          cp "target/${BUILD_TARGET}/release/${BUILD_BINARY}" ".release-router/${BUILD_BINARY}"
          test -x ".release-router/${BUILD_BINARY}"
          rm -rf target
          df -h .

      - name: Fetch macOS engine and ffmpeg
        if: runner.os == 'macOS'
        shell: bash
        env:
          BUILD_TARGET: ${{ matrix.target }}
          GH_TOKEN: ${{ secrets.OCVOICE_RELEASE_TOKEN }}
          FFMPEG_URL_ARM64: https://github.com/OCplan/OCvoice-audio-router/releases/download/ffmpeg-deps-9.0.1/ffmpeg-darwin-arm64.gz
          FFMPEG_URL_X64: https://github.com/OCplan/OCvoice-audio-router/releases/download/ffmpeg-deps-9.0.1/ffmpeg-darwin-x64.gz
          FFMPEG_SHA256_ARM64: 1173162332cf6bdf2cb86a79a00f12ae50e40064243e3c634caeaa182cae52b3
          FFMPEG_SHA256_X64: e605d713d645c8f9ba5555e7b23869a84a2fd69ae94ccc6d369f66f44bf14fca
        run: |
          set -euo pipefail
          DEPS_DIR="${PWD}/.release-sidecars"
          rm -rf "${DEPS_DIR}"
          mkdir -p "${DEPS_DIR}"

          case "${BUILD_TARGET}" in
            aarch64-apple-darwin)
              ENGINE_ASSET="ocvoice-restream-engine-darwin-arm64"
              ENGINE_SHA256="${ENGINE_SHA256_DARWIN_ARM64}"
              FFMPEG_URL="${FFMPEG_URL_ARM64}"
              FFMPEG_SHA256="${FFMPEG_SHA256_ARM64}"
              ;;
            x86_64-apple-darwin)
              ENGINE_ASSET="ocvoice-restream-engine-darwin-x64"
              ENGINE_SHA256="${ENGINE_SHA256_DARWIN_X64}"
              FFMPEG_URL="${FFMPEG_URL_X64}"
              FFMPEG_SHA256="${FFMPEG_SHA256_X64}"
              ;;
            *)
              echo "Unsupported macOS build target: ${BUILD_TARGET}" >&2
              exit 1
              ;;
          esac

          gh release download "${ENGINE_VERSION}" --repo OCplan/OCvoice \
            --pattern "${ENGINE_ASSET}" --dir "${DEPS_DIR}"
          mv "${DEPS_DIR}/${ENGINE_ASSET}" "${DEPS_DIR}/ocvoice-restream-engine"

          # Fail-closed integrity gate. This binary is nested-signed into the .app
          # with our Developer ID, so an unverified download would be redistributed
          # under our signature. Every sidecar requires a matching pinned digest.
          printf '%s  %s\n' "${ENGINE_SHA256}" "${DEPS_DIR}/ocvoice-restream-engine" | shasum -a 256 --check

          curl --fail --location --retry 3 "${FFMPEG_URL}" --output "${DEPS_DIR}/ffmpeg.gz"
          printf '%s  %s\n' "${FFMPEG_SHA256}" "${DEPS_DIR}/ffmpeg.gz" | shasum -a 256 --check
          gzip -dc "${DEPS_DIR}/ffmpeg.gz" > "${DEPS_DIR}/ffmpeg"
          chmod +x "${DEPS_DIR}/ocvoice-restream-engine" "${DEPS_DIR}/ffmpeg"

          # Fail-closed distribution + capability gate. A --enable-nonfree ffmpeg
          # is NOT redistributable (bundling it would violate its license); the
          # engine also needs rtmps/TLS to push to YouTube. Point FFMPEG_URL_* at
          # a GPL (non-nonfree) static build if this fails.
          if "${DEPS_DIR}/ffmpeg" -hide_banner -version 2>/dev/null | grep -q -- "--enable-nonfree"; then
            echo "::error::bundled ffmpeg is --enable-nonfree (not redistributable); set FFMPEG_URL_ARM64/X64 to a GPL build"; exit 1
          fi
          if ! "${DEPS_DIR}/ffmpeg" -hide_banner -protocols 2>/dev/null | grep -qi "rtmps"; then
            echo "::error::bundled ffmpeg lacks rtmps/TLS; the YouTube push would fail"; exit 1
          fi

      # macOS: create .app bundle, sign, notarize, create .dmg
      - name: Create macOS .app and .dmg
        if: runner.os == 'macOS'
        shell: bash
        env:
          ARTIFACT_NAME: ${{ matrix.artifact }}
          BUILD_TARGET: ${{ matrix.target }}
          BUILD_BINARY: ${{ matrix.binary }}
          TAG_NAME: ${{ github.ref_name }}
          APPLE_DEVELOPER_ID_CERT_BASE64: ${{ secrets.APPLE_DEVELOPER_ID_CERT_BASE64 }}
          APPLE_DEVELOPER_ID_CERT_PASSWORD: ${{ secrets.APPLE_DEVELOPER_ID_CERT_PASSWORD }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
          APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY_BASE64 }}
        run: |
          set -euo pipefail
          for required in APPLE_DEVELOPER_ID_CERT_BASE64 APPLE_DEVELOPER_ID_CERT_PASSWORD APPLE_API_KEY_ID APPLE_API_ISSUER_ID APPLE_API_KEY_BASE64; do
            if [[ -z "${!required}" ]]; then
              echo "::error::Required signing/notarization input is missing: ${required}"
              exit 1
            fi
          done
          APP_NAME="OCvoice Audio Router.app"
          VERSION=""
          if [[ "${TAG_NAME}" == v* ]]; then
            VERSION="${TAG_NAME#v}"
          fi

          # --- Code Signing ---
          if [[ -n "${APPLE_DEVELOPER_ID_CERT_BASE64}" ]]; then
            echo "Setting up code signing..."

            # Create temporary keychain
            KEYCHAIN_PATH="${RUNNER_TEMP}/app-signing.keychain-db"
            KEYCHAIN_PASSWORD="$(openssl rand -hex 32)"
            security create-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
            security set-keychain-settings -lut 21600 "${KEYCHAIN_PATH}"
            security unlock-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"

            # Import certificate
            CERT_PATH="${RUNNER_TEMP}/certificate.p12"
            echo "${APPLE_DEVELOPER_ID_CERT_BASE64}" | base64 --decode > "${CERT_PATH}"
            security import "${CERT_PATH}" \
              -P "${APPLE_DEVELOPER_ID_CERT_PASSWORD}" \
              -A \
              -t cert \
              -f pkcs12 \
              -k "${KEYCHAIN_PATH}"
            security set-key-partition-list -S apple-tool:,apple: -k "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
            security list-keychains -d user -s "${KEYCHAIN_PATH}" login.keychain

            # Find signing identity
            IDENTITY=$(security find-identity -v -p codesigning "${KEYCHAIN_PATH}" | grep "Developer ID Application" | head -1 | awk -F'"' '{print $2}')
            echo "Signing with identity: ${IDENTITY}"

            SIGNING_IDENTITY="${IDENTITY}" \
              ENGINE_BINARY="${PWD}/.release-sidecars/ocvoice-restream-engine" \
              FFMPEG_BINARY="${PWD}/.release-sidecars/ffmpeg" \
              ROUTER_BINARY="${PWD}/.release-router/${BUILD_BINARY}" \
              APP_NAME="${APP_NAME}" \
              APP_VERSION="${VERSION}" \
              bash scripts/bundle-macos.sh

            # --- Create DMG ---
            mkdir -p dmg_contents
            cp -R "${APP_NAME}" dmg_contents/
            ln -s /Applications dmg_contents/Applications

            bash scripts/create-dmg.sh dmg_contents "${ARTIFACT_NAME}.dmg"

            # Sign the DMG
            codesign --force --sign "${IDENTITY}" --timestamp "${ARTIFACT_NAME}.dmg"

            # --- Notarization via App Store Connect API key ---
            if [[ -n "${APPLE_API_KEY_BASE64}" ]]; then
              echo "Setting up App Store Connect API key..."
              API_KEY_DIR="${RUNNER_TEMP}/private_keys"
              mkdir -p "${API_KEY_DIR}"
              echo "${APPLE_API_KEY_BASE64}" | base64 --decode > "${API_KEY_DIR}/AuthKey_${APPLE_API_KEY_ID}.p8"

              echo "Submitting for notarization..."
              xcrun notarytool submit "${ARTIFACT_NAME}.dmg" \
                --key "${API_KEY_DIR}/AuthKey_${APPLE_API_KEY_ID}.p8" \
                --key-id "${APPLE_API_KEY_ID}" \
                --issuer "${APPLE_API_ISSUER_ID}" \
                --wait

              echo "Stapling notarization ticket..."
              xcrun stapler staple "${ARTIFACT_NAME}.dmg"
              xcrun stapler validate "${ARTIFACT_NAME}.dmg"
              codesign --verify --strict "${ARTIFACT_NAME}.dmg"

              rm -rf "${API_KEY_DIR}"
            fi

            # Clean up keychain
            security delete-keychain "${KEYCHAIN_PATH}"
          else
            echo "No signing certificate found, creating ad-hoc signed app and unsigned DMG..."
            ENGINE_BINARY="${PWD}/.release-sidecars/ocvoice-restream-engine" \
              FFMPEG_BINARY="${PWD}/.release-sidecars/ffmpeg" \
              ROUTER_BINARY="${PWD}/.release-router/${BUILD_BINARY}" \
              APP_NAME="${APP_NAME}" \
              APP_VERSION="${VERSION}" \
              bash scripts/bundle-macos.sh

            mkdir -p dmg_contents
            cp -R "${APP_NAME}" dmg_contents/
            ln -s /Applications dmg_contents/Applications

            bash scripts/create-dmg.sh dmg_contents "${ARTIFACT_NAME}.dmg"
          fi

      - name: Fetch Windows engine and ffmpeg
        if: runner.os == 'Windows'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.OCVOICE_RELEASE_TOKEN }}
          FFMPEG_URL_WIN: https://github.com/OCplan/OCvoice-audio-router/releases/download/ffmpeg-deps-9.0.1/ffmpeg-win32-x64.gz
          FFMPEG_SHA256_WIN: 85bceaaa95cdad15da33d0f240f346955bbb0450798e2b8e8ffde4b0e59b6fe5
        run: |
          set -euo pipefail
          DEPS_DIR="${PWD}/.release-sidecars"
          ENGINE_ASSET="ocvoice-restream-engine-windows-x64.exe"
          rm -rf "${DEPS_DIR}"
          mkdir -p "${DEPS_DIR}"

          gh release download "${ENGINE_VERSION}" --repo OCplan/OCvoice \
            --pattern "${ENGINE_ASSET}" --dir "${DEPS_DIR}"
          mv "${DEPS_DIR}/${ENGINE_ASSET}" "${DEPS_DIR}/ocvoice-restream-engine.exe"

          # Fail-closed integrity gate (see the macOS step): the engine ships inside
          # the artifact we publish, so an unverified download would be distributed
          # as ours. Unconditional by design.
          printf '%s  %s\n' "${ENGINE_SHA256_WINDOWS_X64}" "${DEPS_DIR}/ocvoice-restream-engine.exe" | sha256sum --check

          curl --fail --location --retry 3 "${FFMPEG_URL_WIN}" --output "${DEPS_DIR}/ffmpeg.gz"
          printf '%s  %s\n' "${FFMPEG_SHA256_WIN}" "${DEPS_DIR}/ffmpeg.gz" | sha256sum --check
          gzip -dc "${DEPS_DIR}/ffmpeg.gz" > "${DEPS_DIR}/ffmpeg.exe"

          # Fail-closed distribution + capability gate (see the macOS step):
          # reject a non-redistributable --enable-nonfree build, require rtmps/TLS.
          if "${DEPS_DIR}/ffmpeg.exe" -hide_banner -version 2>/dev/null | grep -q -- "--enable-nonfree"; then
            echo "::error::bundled ffmpeg is --enable-nonfree (not redistributable); set FFMPEG_URL_WIN to a GPL build"; exit 1
          fi
          if ! "${DEPS_DIR}/ffmpeg.exe" -hide_banner -protocols 2>/dev/null | grep -qi "rtmps"; then
            echo "::error::bundled ffmpeg lacks rtmps/TLS; the YouTube push would fail"; exit 1
          fi

      # Windows: bundle the router and its sidecars in one zip.
      - name: Package Windows binaries
        if: runner.os == 'Windows'
        shell: bash
        env:
          ARTIFACT_NAME: ${{ matrix.artifact }}
          BUILD_TARGET: ${{ matrix.target }}
          BUILD_BINARY: ${{ matrix.binary }}
        run: |
          mkdir -p "${ARTIFACT_NAME}"
          cp "target/${BUILD_TARGET}/release/${BUILD_BINARY}" "${ARTIFACT_NAME}/${BUILD_BINARY}"
          cp ".release-sidecars/ocvoice-restream-engine.exe" "${ARTIFACT_NAME}/ocvoice-restream-engine.exe"
          cp ".release-sidecars/ffmpeg.exe" "${ARTIFACT_NAME}/ffmpeg.exe"
          powershell.exe -NoProfile -Command \
            "Compress-Archive -Path '${ARTIFACT_NAME}' -DestinationPath '${ARTIFACT_NAME}.zip' -Force"

      - name: Upload artifact (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: "${{ matrix.artifact }}.dmg"

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: "${{ matrix.artifact }}.zip"

  release:
    needs: [version-gate, build]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Audio Router ${{ github.ref_name }}"
          body: |
            ## OCvoice Audio Router

            Download the right version for your system:

            | Platform | File |
            |---|---|
            | **macOS (Apple Silicon — M1/M2/M3/M4)** | `OCvoice-Audio-Router-macOS-arm64.dmg` |
            | **macOS (Intel)** | `OCvoice-Audio-Router-macOS-x64.dmg` |
            | **Windows** | `OCvoice-Audio-Router-Windows.zip` |

            ### macOS
            1. Download and open the `.dmg` file
            2. Drag **OCvoice Audio Router** to the **Applications** folder
            3. Double-click to launch — the app runs in the background via the menu bar

            ### Windows
            1. Download and extract the `.zip` file
            2. Run `ocvoice-audio-router.exe` inside the extracted folder
            3. Click "More info" → "Run anyway" if SmartScreen blocks it
            4. Done — starts automatically on login

            ### YouTube setup
            On the computer sending your video, open **YouTube setup / Opsætning**
            from the app menu. Enter the one-time computer code from OCvoice,
            start the connection, and follow the OBS fields on the setup page.
            Danish and English instructions are included. Check broadcast
            visibility and viewer links in OCvoice before sending video.

            This release includes the restream engine and video support. Keep
            all three Windows files together. macOS apps are signed and notarized.
            The Windows ZIP is not Authenticode-signed.

            Bundled video component source and build information:
            https://github.com/OCplan/OCvoice-audio-router/releases/tag/ffmpeg-deps-9.0.1
          files: artifacts/**/*
